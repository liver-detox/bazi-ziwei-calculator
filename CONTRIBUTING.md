# 贡献指南

感谢你帮助改进赛博大师·八字与紫微排盘计算器。当前项目面向本机使用，八字与紫微斗数基础计算同等重要。

## 开始之前

1. 安装 Node.js 24 或更高版本。
2. 在项目目录运行 `npm ci`。
3. 修改应保持范围清楚，并避免无关格式化或大规模重写。
4. 提交前运行 `npm run test:release`。
5. 性能相关改动另运行 `npm run test:performance`，如实记录结果；性能基准目前不阻塞首发。

## 只接受合成测试资料

公开测试、夹具、演示、截图和问题复现只接受从零构造、明确标为 `synthetic` 或 `demo` 的资料。不得提交真实案例，即使已经删除姓名、打码或修改部分字段也不可以。

合成资料不得包含真实姓名、地点、联系方式、经纬度、私密身份、本机路径或真实资料的变体。添加合成资料时，请只保留测试所需字段，并说明它要覆盖的边界。

禁止把真实出生资料、私密导出、数据库、日志、环境变量、密钥或含个人信息的截图放入提交或公开 Issue。

## 代码与测试要求

- 功能或缺陷修复应先加入能复现预期行为的测试。
- 正确性、类型检查和构建必须通过；不得用放宽断言、延长超时或降低并发掩盖问题。
- 不得为了性能降低严格重读、篡改检测、脱敏或原子保存要求。
- 不要引入依赖 macOS 路径分隔符的生产代码；新增平台相关行为时说明 macOS 与 Windows 差异。
- 新增第三方代码、数据或素材时，必须说明来源和许可证。

安全问题请遵循 [SECURITY.md](SECURITY.md)，隐私规则见 [PRIVACY.md](PRIVACY.md)。
