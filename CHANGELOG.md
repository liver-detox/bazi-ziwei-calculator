# 更新记录

本项目的公开版本变化记录在这里。

## [0.4.0] - 2026-09-17

同宫分层展示与导出可靠性提升。八字与紫微核心引擎版本保持不变。

- 紫微页面、AI 文本、TXT 和打印统一按固定本命宫位连续展示本命、大限、流年星曜与四化，减少来回查找和重复星表；缺失、冲突或无法唯一归宫的记录明确提示。
- 将外部来源冻结的起运、紫微安星与四化样本纳入公开测试；[核验说明](docs/verification/independent-chart-rules.md)保留流派差异及事后补证边界，不扩张为全面准确性或预测效果承诺。
- 紫微跨大限年份显示春节交限公历日、前后大限及本命落宫，区分童限；缺失或不一致的旧资料明确显示不可用。
- 为 JSON 导出和当前文本呈现分别增加可重现的源码短标识；旧文档缺失时显示未知，不将当前源码身份追记为旧排盘的计算来源。
- 修复出生当年起运时无法创建案例的问题；总览、八字详盘与大运流年页统一显示“出生当年起运”，避免倒置的年龄与年份。
- 大运流年页与 AI 文本按已算起运时刻显示公历年内的日期分段，明确年表索引、立春年界与紫微年度代表盘的范围。
- AI 文本、TXT 与打印补齐以地支、宫名表达的十二宫关系、对宫、三合及所选大限/流年宫名，逐项标明四化和本命落宫；区分星曜字段为空与缺失，并列明文本展开年份和完整 JSON 的年份范围。
- 完整 JSON 及共用 AI 文本、TXT、打印内容现在携带当前计算审计、时间证据限制和允许分析范围；约略时间与时辰不确定的限制不再遗漏。
- 明确所提供时间未经独立核验，本次计算不自动校时；旧版 V1 缺少证据信息时显示未知。
- 吸收公开主线的姓名文案修正：“姓名或代号（可选，复制与导出会包含）”，不要求填写真名。
- 明确 JSON 的单向兼容：新读取器可读旧 V1；新增 `evidence`、`exportSourceId` 会被旧严格读取器拒绝，接收新导出前须先升级读取器。旧文件的缺失证据与原导出来源仍为未知。
- 平台验证按版本标注；历史 Windows/Safari 验证不延伸为本版已验证声明。
- 定向更新 Fastify 至 5.12.1、Vitest 至 4.1.11，并将两条 fast-uri 依赖更新至 3.1.6 / 4.1.3，覆盖对应安全公告；不变更八字与紫微排盘引擎。

## [0.3.2] - 2026-09-04

- 在出生日期旁醒目标明“公历（默认）”或“农历”，初次使用也能直接看懂当前历法。
- 农历日期改用适合农历规则的输入方式，支持月三十等有效日期。
- 紫微详盘可从所选年份起一键添加连续 5 年或 10 年，不再需要逐年操作。
- 切换公历与农历时自动清空旧日期和闰月状态，避免混用。

## [0.3.1] - 2026-08-28

- 将默认使用路径收缩为“输入资料 → 查看结果 → 必要时选择候选 → 复制给 AI”；多候选确认只保留一个动作，补充说明改为可选。
- 晚子时录入统一保留双候选；修改旧修订时也不再继承已从界面移除的单策略。
- 默认界面不再展示核验状态、允许模式和原始差异代码；审计等级、修订与指纹仍保留在折叠的高级信息或底层兼容数据中。
- 简化时间类型提问、按钮文案和 README 首屏，合并重复的隐私与平台说明，并加入简短的 AI 文本示例。

## [0.3.0] - 2026-08-26

- 在结果页增加一键“复制给 AI”：普通结果可直接复制，未决的晚子时或多候选结果会先进入核验；剪贴板不可用时自动回退到“核验与导出”。
- 优化紫微流年星曜的宫位标签与 AI 文本说明，减少交给大模型时的歧义；每次仍只导出一个已选定候选。
- Windows 11 + Chrome 已通过三个合成案例的人工验收；Microsoft Edge 未纳入本轮支持范围。
- 将锁文件中的开发依赖 `nanoid` 安全补丁更新至 `3.3.18`。
- 增加面向大模型的 AI 文本复制、TXT 下载与可用时的系统分享（可能发送 TXT 文件或同一文本）；复制、TXT、分享和打印视图同源。“打开打印”调用系统打印，浏览器或系统提供时可在打印窗口选择存为 PDF；完整 ChartDocument V1 JSON 下载保持不变。

## [0.2.0] - 2026-08-19

- 完成单文件版本化 JSON 导出，每个文件只包含一个已选定候选，并保留姓名或代号、出生资料及对应的八字与紫微斗数结果；提供完全合成的公开示例及校验。
- 增加 Windows 就绪候选：提供启动器和公开 CI 配置，用于检查中文/空格候选路径并运行发布门；尚未构成正式 Windows 支持，仍待 Windows 11 浏览器与本机体验人工验收。

## [0.1.0] - 2026-08-11

首个公开版本：

- 提供八字与紫微斗数基础排盘、核验、修订历史和本地导出流程；
- 公开演示使用三个从零构造的合成案例；公开测试只使用合成输入，或来源与许可证明确的公开上游夹具；
- 提供 macOS 本地启动方式，Windows 支持仍在计划与验收阶段；
- 正确性测试、类型检查和构建作为发布硬门，性能基准透明记录但暂不阻塞首发；
- 增加公开隐私、安全、贡献和第三方归属说明。
